class User {
    constructor(id, userName, email, password, token) {

        this.id = id;
        this.userName = userName;
        this.email = email;
        this.password = password;
        this.token = token;
    }
}
export default User;